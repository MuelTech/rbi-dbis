# Task 35: Refactor PDF report to portrait with simplified columns

## Status
DONE

## Summary
Refactored the PDF generator from landscape to portrait A4 orientation and simplified the table from 10 columns to 5 by combining name fields (lastName, firstName, middleName) into a single "Name" column formatted as "Last, First Middle". Removed Age, Contact, and Status columns. Updated footer to right-align the "Generated by" text and used dynamic `pageWidth` instead of hardcoded values.

## Changes
- **Orientation**: `"l"` → `"p"` (landscape → portrait)
- **Header centering**: Replaced hardcoded `148` x-position with `pageWidth / 2`
- **Table columns**: 10 columns → 5 columns (No., Name, Sex, Address, Signature)
- **Column widths**: Adjusted to fit portrait width (total ~180mm within 190mm usable)
- **Footer**: `generatedBy` text right-aligned instead of center; `pageWidth` used dynamically

## TypeScript Check
`npx tsc --noEmit` passed with no errors.

## Commit
`da54193` — `refactor(pdf): portrait orientation with simplified columns`

## Concerns
None.
